// Copyright Epic Games, Inc. All Rights Reserved.


#include "TestProjectGameModeBase.h"
void ATestProjectGameModeBase::BeginPlay() {
	Super::BeginPlay();
	//auto Timer = GetWorld()->SpawnActor<ATimerActor>(FVector::ZeroVector, FRotator::ZeroRotator);

	//Timer->SetTimer(1, 15);
	//Timer->CountDownStart();
	//Timer->TimerEndDelegate.AddLambda([this]()->void {

	//	UE_LOG(LogTemp, Error, TEXT("TimerEnd"));
	//});
}